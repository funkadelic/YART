# Commits listed here are pure formatting rewrites with no behaviour change.
# Run `git config blame.ignoreRevsFile .git-blame-ignore-revs` once so blame
# skips them and keeps pointing at the commit that wrote each line.

# Formatted the whole repository with Prettier.
f2caaaa0739552d6c3eb5a856dea728f261c396d
